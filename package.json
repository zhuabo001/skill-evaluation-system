{
  "name": "skill-studio",
  "version": "0.0.0",
  "private": true,
  "description": "Skill Studio monorepo — local desktop tooling for engineering AI skills (V1 Phase 0 skeleton).",
  "packageManager": "pnpm@10.6.1",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "echo \"lint not configured in Phase 0\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
